import { FilterOutContentFieldPipe } from './filter-out-content.pipe';

describe('FilterOutContentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOutContentFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
